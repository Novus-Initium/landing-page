---
// Define the array of sections
const sections = [
  {
    title: "Impact Measurement",
    description:
      "We're deploying Impact Measurement Protocols (IMPs) on Doric L2, built on Scaffold Eth2 and based on AlloV1 Grants Stacks.  Now you can easily participate in or run your very own Quadratic Funding Grant Rounds for your community.",
    imgSrc: "/images/thumbnail6.svg",
    imgAlt: "IMPs",
    bgColor: "bg-green-400", // You can also dynamically set background colors if needed
  },
  {
    title: "Consultation & Support",
    description:
      "Do you need some tips and advice for your organization?  We can help.",
    imgSrc: "/images/thumbnail5.svg",
    imgAlt: "Thumbnail for Decentralized exchanges",
    bgColor: "bg-lila-500",
  },
];
---

<section class="relative flex items-center w-full">
  <div
    class="relative items-center w-full mx-auto 2xl:max-w-7xl divide-y-2 border-b-2 divide-black 2xl:border-x-2 border-black">
    <div
      class="grid grid-cols-1 md:grid-cols-2 divide-y divide-black md:divide-y-0 md:divide-x-2">
      {
        sections.map((section) => (
          <div class="grid grid-cols-1 divide-y-2 divide-black">
            <div class="relative p-8 lg:px-20 py-12 items-center gap-12 h-full lg:inline-flex bg-white">
              <div class="max-w-xl text-center mx-auto">
                <p class="text-3xl lg:text-5xl 2xl:text-4xl tracking-tight font-medium text-black">
                  {section.title}
                </p>
                <p class="mt-4 text-lg tracking-wide text-black font-normal mx-auto">
                  {section.description}
                </p>
              </div>
            </div>
            <div class={`w-full aspect-square h-full ${section.bgColor}`}>
              <img
                class="border-b-0 object-cover"
                src={section.imgSrc}
                alt={section.imgAlt}
              />
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
